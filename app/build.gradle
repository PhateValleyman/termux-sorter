plugins {
    id 'com.android.application'
}
android {
    namespace "com.umbrellacorp.termuxsorter"
    compileSdk 34
    defaultConfig {
        applicationId "com.umbrellacorp.termuxsorter"
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName "1.0"
    }
    signingConfigs {
        release {
            storeFile file("/data/data/com.termux/files/home/.config/keys/my.keystore")
            storePassword "24nascr"
            keyAlias "valleyman"
            keyPassword "24nascr"
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
        }
    }
}
